# Schemas

Versioned JSON Schema contracts are packaged under
`src/ai_parametric_architect/contracts/schemas` (world model) and
`src/ai_parametric_architect/intent/schemas` (design intent), so installed
applications use the same files as tests and source checkouts. Structural
compatibility is explicit; schema files are not generated from a second Python
model definition.
